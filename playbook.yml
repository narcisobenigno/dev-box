- hosts: localhost
  connection: local
  vars:
    home: "{{ lookup('env','HOME') }}"
    dev_box_dir: "{{ lookup('env','DEVBOX') }}"
  roles:
  - git
  - tmux
  - { role: vim, tags: ['vim'] }
  - utilities
  - mac-deps
