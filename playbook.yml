- hosts: localhost
  connection: local
  vars:
    home: "{{ lookup('env','HOME') }}"
    dev_box_dir: "{{ lookup('env','DEVBOX') }}"
    private_repo: "{{ lookup('env','PRIVATE_GIT_REPO') }}"
  roles:
  - git
  - tmux
  - { role: vim, tags: ['vim'] }
  - pass
  - utilities
  - mac-deps
